description = "Different Integration Implementations (Local and non-local)"

configurations {

    compile.extendsFrom(oauth2Domain, oauth2Test)
}

dependencies {

    compile project(":mc-oauth2-spring-integration-api")
}
