description = "Implements Local Integration (in-memory)"

configurations {

    compile.extendsFrom(oauth2Domain)
    testCompile.extendsFrom(oauth2Test)
}

dependencies {

    compile project(":mc-oauth2-spring-integration-api")

    testCompile project(":mc-oauth2-spring-common-domain-test")
}
